# deploy/docker

Environment-specific Compose overrides (`docker-compose.staging.yml`,
`docker-compose.prod.yml`) and container entrypoint helpers, added in the
DevOps stage (`docs/spec/08-devops.md`). The base `docker-compose.yml` at
the repository root is sufficient for local development.
